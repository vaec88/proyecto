<%= stylesheet_link_tag "tablas.css", :media => "screen" %>
<div id="tabla">
  <table class="bordered">
    <tr>
      <th><div class="titulos"><p>Cantidad de Abono</p></div></th>
      <th><div class="titulos"><p>Fecha (aaaa/mm/dd)</p></div></th>
      <th><div class="titulos"><p>Acciones</p></div></th>
    </tr>
    <%@honorarios.each do |h| %>
      <tr>
        <td><div class="etiquetas_left"><%= label_tag nil, h.hon_cantidad %></div></td>
        <td><div class="etiquetas_left"><%= label_tag nil, h.hon_fecha.strftime('%Y-%m-%d') %></div></td>
        <td><%= label_tag nil, (link_to (image_tag "contenido/edit16.png"), :action => "edit", :id => h.id) %><%= label_tag nil, (link_to (image_tag "contenido/delete16.png"), h, :method => "delete", :confirm => "¿Esta seguro?") %></td>
      </tr>
    <% end %>

    <%= form_for @honorario do |h|%>
      <tr>
        <td><%= h.text_field :hon_cantidad, :style=>"width:100px" %></td>
        <td><%= h.text_field :hon_fecha, :style=>"width:150px" %></td>
        <%= hidden_field_tag :construction_id, @inmueble.id %>
        <td><%= label_tag nil, image_submit_tag("contenido/add2-16.png")%></td>
      </tr>
    <%end%>
      
  </table>
</div>

<%#= form_for @honorario do |h|%>
<!--  <div id="formulario">
    <table>
      <tr>
        <td><%#= h.label "Cantidad de Abono: " %></td>
        <td><%#= h.text_field :hon_cantidad %></td>
      </tr>
      <tr>
        <td><%#= h.label "Fecha de Transaccion: " %></td>
        <td><%#= h.text_field :hon_fecha %></td>
      </tr>
      <%#= hidden_field_tag :construction_id, @inmueble.id %>
      <%#= @inmueble.id %>
      <tr>
        <td colspan="2" class="boton"><%#= h.submit "Guardar"%></td>
      </tr>
    </table>
  </div>-->
<%#end%>

<%#= link_to 'Agregar abono', new_honorary_path(:construction_id => @inmueble.id) %>